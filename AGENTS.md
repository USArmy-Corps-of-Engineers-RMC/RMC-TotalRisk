<!-- GENERATED FILE — do not edit. AGENTS.md is produced from CLAUDE.md by scripts/sync-agents-md.py. Edit CLAUDE.md and rerun the script. -->

﻿# RMC-TotalRisk

## Technical Authority and Algorithm Change Control (NON-NEGOTIABLE)

Haden Smith is the final technical and numerical authority for this repository. AI agents,
including Codex and Claude, must never change an established algorithm, formula,
probability-combination rule, numerical method, convergence rule, default tolerance,
random-seed behavior, clipping or normalization policy, or reference-result contract without
Haden Smith's explicit prior approval. A failing test authorizes diagnosis and a proposed fix
only; it does not authorize tuning or replacing the algorithm. When an agent's reasoning
conflicts with Haden Smith's direction or domain judgment, the agent must stop, present the
evidence, and request a decision. AI confidence is not technical authority.

## Project Overview

Monte-Carlo-based quantitative risk analysis framework for dam and levee safety, developed by the USACE Risk Management Center. **This software is used for life-safety flood risk assessments worldwide.** Code quality is paramount.

- **Version:** 1.1.0 (in development on branch `v1.1-development`; v1.0 is the released desktop product — see the README)
- **Language:** C# — model library first (no UI, no IO frameworks); UI/App layers come in later phases
- **Framework:** .NET 10
- **Primary consumers:** the future RMC-TotalRisk desktop app, USACE practitioners, U.S. Federal agencies, consultants, academics, AWS/agentic headless callers
- **Porting source:** the legacy repos remain at `C:\GIT\RMC-TotalRisk-Dev` (VB.NET v1.0 engine, partial C# port `RMC.TotalRisk.IO`, and the `Test_TotalRisk` Monte Carlo oracle suite). This repo is the clean v1.1 home; the Dev repo is reference-only.

## Headless-Compute Philosophy (non-negotiable)

`RMC.TotalRisk.dll` is a headless compute library: typed system definition + seed in → reproducible typed results out. Callable from AWS workers, web services, and agentic AI tools. Every design decision serves that:

- **No UI frameworks.** No WPF, no `System.Windows.*`, no `Dispatcher`. Target `net10.0`, never `net10.0-windows`.
- **No singletons.** No `Project.Current` global state. Analyses take inputs via constructor/method args and return pure result objects.
- **No direct file I/O.** Callers pass already-parsed data. In-memory serialization only: model *definition* types use `ToXElement()` / ctor-from-`XElement` (the canonical-hash identity surface); *results* containers use System.Text.Json (`ToJson()`/`FromJson()` + compressed-bytes overloads) — v1.0's BinaryFormatter BLOBs are not ported, and v1.0 projects re-run their analyses in v1.1. No SQLite, no `File.*` in the model lib.
- **Deterministic entry points.** Explicit seeds, typed inputs/outputs, no dialogs. Same inputs + same seed → bit-identical results at any thread count.
- **Content-based seed identity.** Monte Carlo seeds derive from SHA-256 canonical content hashes (XML canonicalization over `ToXElement()` through audited strip rules) plus occurrence indices — renaming, canvas moves, or reordering never change results. Normative spec: `docs/requirements/MODEL_LIBRARY_ARCHITECTURE.md` §5.5. **Landing checklist for every new model property:** classify it compute-relevant (hashed) or metadata (add to `CanonicalizationRules`), extend the kitchen-sink rename/reorder invariance test, and never rename or reorder existing serialized attributes — `ToXElement()` is the identity surface and hashes are contract. (Exception ratified v0.9, extended v0.12: `SystemComponent` and `CompositeConsequence` split identity from persistence — their `CanonicalHash()` hashes a projected identity form, because element-graph XML carries link Guids/names and composite ByReference forms carry `FunctionReference` markers that must never be a hash surface; `RiskFunctionBase.CanonicalHash()` is virtual for exactly these container overrides.) **Every new concrete function type also handles both `RiskSerializationMode`s and adds its `RiskFunctionFactory` case and its `Core.Enums` discriminator member** (`HazardFunctionType`/`TransformFunctionType`/`ResponseFunctionType`/`ConsequenceFunctionType`; every new element type adds its `RiskElementFactory` case and `RiskElementType` member) alongside the matrix row and invariance registration. Discriminators are runtime-only — never serialize one, or the hash surface moves.
- **`INotifyPropertyChanged` is allowed.** It is a passive contract; headless callers don't subscribe. It gives the future WPF UI layer a clean data-binding story.

**Dependency rule (MANDATORY):** `RMC.TotalRisk.dll` references only `RMC.Numerics`. Never `RMC.BestFit`, never UI/IO frameworks, never SQLite. BestFit fitted results are imported as **already-parsed Numerics artifacts** (`UnivariateDistributionBase`, `ParameterSet[]`, `UncertaintyAnalysisResults`, `UncertainOrderedPairedData`) — reading `.rmcbf` files is a UI-layer concern. The validation script fails on any `RMC.BestFit`, `System.Windows`, or SQLite reference. See `docs/requirements/SHARED_FUNCTIONS_STRATEGY.md`.

## Project Architecture

```
Numerics.dll (RMC.Numerics)         ← distributions, uncertain paired data, functions,
    ↑                                 sampling (LHS/stratification/bootstrap/MCMC), math
RMC.TotalRisk.dll                   ← model library (input functions, risk components,
    ↑                                 Monte Carlo risk engine, results)
Future consumers                    ← RMC.TotalRisk.UI → RMC-TotalRisk App; REST API; agents
```

**Interim dependency note:** all four csproj files (the library, both test projects, and `scripts/perf/PerfHarness` — the harness carries one extra `..\`) reference the sibling build `..\..\..\numerics\Numerics\bin\Debug\net10.0\Numerics.dll` via `<HintPath>`; switch to the `RMC.Numerics` PackageReference (already declared in `Directory.Packages.props`, local feed `C:\GIT\numerics\packages`) once the user pushes and releases ≥ 2.2.0 (the Phase 8 implementation landed in the numerics repo; the release + package switch is the remaining exit gate).

**Namespace map** (folders mirror namespaces exactly; no types in the bare `RMC.TotalRisk` root namespace). Authoritative layout: `docs/requirements/MODEL_LIBRARY_ARCHITECTURE.md` §3. Summary:

**Vocabulary rules (arch doc v0.8, refined v0.9):** no root `IModel` abstraction — the kernel contract is `IRiskFunction` because TotalRisk's engine consumes functions by role, never "any model". The v0.8 "element purge" is narrowed by v0.9: the ban covers the wpf-framework `ProjectInterfaces.IElement` *wrapper* lingo, while the headless risk-graph node contract is **`IRiskElement`** (Hydrologics `IBasinElement` mirror) with concrete `HazardElement`/`TransformElement`/`ResponseElement`/`ConsequenceElement` inside `ComponentGraph` — the formal DAG each `SystemComponent` owns. `RiskDiagram` stays reserved for the future UI/App controls (DAG.dll/DAGControls remain UI-only; the model lib never references them).

**Plural-segment rule (v0.10):** every namespace segment that could shadow a BCL or Numerics identifier is plural. `RMC.TotalRisk.System` would hide the BCL `System` namespace from inside every `RMC.TotalRisk.*` namespace (CS0234) and break consumers writing `using RMC.TotalRisk;` (CS0104); a singular `Transform` segment hides `Numerics.Data.Transform`. Hence `Systems`, `Hazards`, `Transforms`, `Responses`, `Consequences` — the same collision-dodge Hydrologics uses for `Reservoirs`. Type names stay singular.

| Namespace | Contents |
|---|---|
| `RMC.TotalRisk.Core` | `RiskFunctionBase`, `CanonicalContentHasher`, `CanonicalizationRules` (`ModelRules`), `SeedHelpers`, `ByteArrayComparer`, `SerializationUtilities`, `FunctionHelpers`, `TabularUncertainty`, `ParametricPosterior` (Phase 1) |
| `RMC.TotalRisk.Core.Enums` | Every enum, one per file: `SamplingScheme`, `FunctionUncertainty`, `FailureModeMethod`, `DependencyType`, `JointConsequenceType`, `RiskType`, `HazardDimension`, `RiskSerializationMode`, `RiskIntegrand` (adaptive-refinement objective; Phase 4), `VegasTailFocusMode` (Phase 4b), and the runtime discriminators `HazardFunctionType`/`TransformFunctionType`/`ResponseFunctionType`/`ConsequenceFunctionType`/`RiskElementType`/`RiskAnalysisMode`. **`RiskIntegrand`/`VegasTailFocusMode` are `RiskAnalysisOptions` fields — hashed like the other options, unlike the runtime discriminators** |
| `RMC.TotalRisk.Core.Interfaces` | Every interface: `IRiskFunction`, `IHazardFunction`/`IUnivariateHazardFunction`/`IBivariateHazardFunction`, `ITransformFunction`/`IBivariateTransformFunction`, `IResponseFunction`/`IBivariateResponseFunction`, `IConsequenceFunction`/`IBivariateConsequenceFunction`, `IRiskFunctionResolver`, `IRiskElement`, `IRiskElementNameAuthority`; `IAnalysis` (Phase 4+) |
| `RMC.TotalRisk.RiskFunctions` | `RiskFunctionFactory`, `RiskFunctionResolver`, `FunctionEntry`, `CompositeSupport`, `BivariateTableSupport` |
| `RMC.TotalRisk.RiskFunctions.Hazards` | `HazardFunctionBase`/`UnivariateHazardBase`, `TabularHazard`, `ParametricUnivariateHazard`, `NonparametricHazard` (Phase 2+), `CompositeHazard` + `WeightedHazardFunction` (Phase 9), `BivariateHazard` (Phase 11) |
| `RMC.TotalRisk.RiskFunctions.Transforms` | `TransformFunctionBase`, `TabularTransform`, `LinearTransform`, `PowerTransform` (Phase 2+), `CompositeTransform` + `WeightedTransformFunction` (Phase 9), `BivariateTransform` (Phase 11) |
| `RMC.TotalRisk.RiskFunctions.Responses` | `ResponseFunctionBase`, `TabularResponse`, `ParametricResponse`, `NonFailResponse` (Phase 2+), `CompositeResponse` + `WeightedResponseFunction` (Phase 9), `BivariateResponse` + `WeightedHazardLevel` (Phase 11) |
| `RMC.TotalRisk.RiskFunctions.Responses.Trees` | Common branching-response contracts: `TreeFragment`, `TreeLinkMode`, `TreeDeletePolicy`, `TreeNodeReference`, `ResponseBranchDescriptor`, `ResponseBranchSample`, `IBranchingResponseFunction`, `ProbabilitySource` + `ProbabilitySourceKind` (the shared tree probability-source pair, promoted at Phase 10B slice 0a), the lifted internal compilation machinery (`ITreeComputeSource`, `TreePlanDependencies`, ambient `TreeCompilationScope`, `TreeCanonicalPaths`, `TreeMutationSnapshot`, `TreeDependencyCollector`, `IProjectedIdentityResponse`), and the node-importance analysis (`TreeNodeImportance`, `TreeNodeImportanceOptions`, `TreeNodeImportanceResult`, `TreeNodeImportanceEntry`) (Phases 10A–10B) |
| `RMC.TotalRisk.RiskFunctions.Responses.EventTrees` | `EventTreeResponse`, `EventTree`, `EventNodeBase`, `InitiatingNode`, `ChanceNode`, `RemainderNode`, `EventTreeLinkNode` (Phase 10A complete) |
| `RMC.TotalRisk.RiskFunctions.Responses.FaultTrees` | `FaultTreeResponse`, `FaultTree` (+ authoring partial), `FaultTreeNodeBase`, `FaultTreeGateNode`, `FaultTreeBasicEventNode`, `FaultTreeHouseEventNode`, `FaultTreeTransferNode`, `FaultTreeGateType`, `FaultTreeCutSet`, `FaultTreeCutSetEvent`, the internal occurrence plan/read scope, and the internal ROBDD kernel (`FaultTreeBdd`, `FrozenFaultTreeBdd`, `FaultTreeBddBudgetException`) (Phase 10B complete) |
| `RMC.TotalRisk.RiskFunctions.Consequences` | `ConsequenceFunctionBase`, `TabularConsequence`, `ParametricConsequence` + `ClampedPowerFunction`, `CompositeConsequence` + `WeightedConsequenceFunction` + `CompositeUnivariateFunction` (Phase 2+; parametric/composite landed pre-Phase-4), `BivariateConsequence` (Phase 11) |
| `RMC.TotalRisk.Systems` | Reserved for the multi-component system root (the Hydrologics `BasinModel` analog); empty today |
| `RMC.TotalRisk.Systems.Components` | `SystemComponent` (owns a `ComponentGraph`; projects `FailureModes`; identity-form `CanonicalHash`; `AssignOccurrenceIndices`), `FailureMode` (`ResponseStage` chains + ordered multi-type consequences + structural binding), `ResponseStage` (Phase 3) |
| `RMC.TotalRisk.Systems.Components.Graph` | `RiskElementBase`, `HazardElement`/`TransformElement`/`ResponseElement`/`ConsequenceElement`, `RiskConnection`, `ComponentGraph`, `RiskElementFactory`/`RiskElementResolver`, `HazardSourceOption` (Phase 3) |
| `RMC.TotalRisk.Analyses` | `AnalysisBase` support, `RiskAnalysis` engine + `RiskAnalysisOptions` (reliability is a `RiskAnalysisMode`, not a second type), `CostBenefitAnalysis` (Phase 4+) |
| `RMC.TotalRisk.Results` | `SampledComponent`/`SampledFailureMode`, `ComponentRiskOutput`, results containers (Phase 4), `SampledBivariateHazard` (Phase 11) |

## Layer Boundaries (MANDATORY reading before UI work)

Normative spec: `docs/requirements/MODEL_LIBRARY_ARCHITECTURE.md` §8 (v0.11). Summary:

- **Input functions are stored by the consuming layer, one item each, and referenced by `IRiskFunction.Id`** (a Guid, serialized but stripped from hashing). Names are a lenient fallback only — BestFit's name-based references are its own documented regret.
- **`SystemComponent`s are owned by their `RiskAnalysis`**, not independently creatable in the UI/App. `RiskAnalysis.ToXElement()` will serialize **options only** and take components + results through its constructor — the BestFit `new UnivariateAnalysis(dist, xElement, results)` shape.
- **`RiskSerializationMode`**: `SelfContained` (default; headless, oracles, API) writes function content inline; `ByReference` writes `<FunctionReference Id Name/>` markers that an `IRiskFunctionResolver` re-attaches to the **live** stored instances. The mode is persistence only — it can never move a canonical hash or a seed, and that is directly tested.
- **Graph editors use the authoring surface**, never their own mapping: `RiskElementFactory.CreateForFunction`/`Create`, `IRiskElement.TryAssignFunction`, `ComponentGraph.GetUniqueName`/`GetAvailableHazardSources`, `SystemComponent.GetReferencedFunctions`.
- The model library must stay usable alone: build a system, validate, and hash with no store, no resolver, and no consuming layer in the call path.

## Test Project Architecture

Three projects split by scope and speed:

```
RMC.TotalRisk                 ← the model library
RMC.TotalRisk.Tests           ← fast programmatic unit tests (seconds)
RMC.TotalRisk.Verification    ← Monte Carlo verification vs legacy oracles (minutes)
                                NOT built in Release (omitted .sln Build.0 line)
                                [assembly: TestCategory("Verification")]
```

### Test classification rule

- **Programmatic unit tests → `RMC.TotalRisk.Tests`.** Constructor validation, property round-trips and change notification, `Validate()` matrices, XML serialization round-trips, canonical-hash invariance, simple calculation checks at known points, exception throws. Small inline fixtures only.
- **Computational verification → `RMC.TotalRisk.Verification`.** Monte Carlo oracle parity (converted from legacy `Test_TotalRisk`), engine-vs-oracle statistical asserts, reproducibility-at-scale, LHS variance reduction. See the Verification Workflow below and `docs/verification.md`.

### Commands (verified on the .NET 10 SDK)

| Intent | Command |
|---|---|
| Fast PR gate / dev loop | `dotnet test -c Release` (Verification not built in Release) |
| Fast loop, Debug binaries | `dotnet test src/RMC.TotalRisk.Tests` |
| Verification suite (deliberate, per change area) | `dotnet test src/RMC.TotalRisk.Verification` |
| Everything, Debug | `dotnet test` |
| Doc/namespace/dependency/traceability validation | `.\scripts\validate-code-xml-docs.ps1 -Configuration Debug` |
| Verification traceability only | `.\scripts\validate-verification-traceability.ps1` |
| Regenerate AGENTS.md after editing this file | `py scripts/sync-agents-md.py` |
| Perf measurement (ONE fixture per invocation) | `dotnet run -c Release --project scripts/perf/PerfHarness -- F1` (fixtures F1–F7; single rep default, `--reps 3` for committed table rows; byte-gate hashes + allocation counters in `scripts/perf/RESULTS.md`) |

> **Long-run workflow rule (user directive, 2026-07-23):** run verification families and perf fixtures **isolated, one at a time** — `--filter "ClassName~<Family>"` per run, one harness fixture per command. Never launch the whole verification suite as one blocking run mid-session; at phase-close gates, run the families sequentially.

> **⚠ .NET 10 `dotnet test` gotcha:** the SDK drives MSTest.Sdk projects through Microsoft.Testing.Platform. `dotnet test --filter "TestCategory!=Verification"` is **silently ignored** (the classic VSTest argument is not forwarded). Passing the filter after `--` works per-assembly but fails the run when an assembly matches zero tests. Therefore: **scope by configuration or by project path** as in the table above; do not rely on category filters at the CLI. The assembly-level `[TestCategory("Verification")]` remains useful for IDE/explorer filtering.

### Shared MSTestSettings.cs

`[assembly: Parallelize(Scope = ExecutionScope.MethodLevel)]` lives in `src/RMC.TotalRisk.Verification/MSTestSettings.cs` and is **linked** into `RMC.TotalRisk.Tests`. Tests must be independent of execution order and share no mutable static state.

### Coverage targets

- `RMC.TotalRisk.dll` — >90% line coverage from `RMC.TotalRisk.Tests` alone (the fast suite).
- **Every new public class must have a corresponding `<ClassName>Tests.cs`** in `RMC.TotalRisk.Tests`, in a folder mirroring the source folder. A new class without tests is not ready to commit.
- **Every ported compute type must additionally gain verification coverage** (its oracle family in `RMC.TotalRisk.Verification`) by the phase that converts its legacy scenarios. Track gaps in the Ported Types Matrix below.
- **Every legacy `Test_*` method and every verification-report scenario remains classified** in `docs/verification/legacy-traceability.csv`; the validator fails on an untracked source method or stale current-test target.

### Regression check on every library change (MANDATORY)

```bash
dotnet build          # zero warnings required
dotnet test -c Release
```

If the change touches a verified family, also run its verification class (scope by project path + class): `dotnet test src/RMC.TotalRisk.Verification -- --filter "ClassName~JointFailuresVerification"`.

## Repository Layout

```
RMC-TotalRisk/                      ← repo root (github.com/USACE-RMC/RMC-TotalRisk)
├── RMC-TotalRisk.sln               ← solution; Core/Tests/Verification solution folders
├── CLAUDE.md                       ← Codex sibling (source of truth — edit it, then regenerate this file)
├── AGENTS.md                       ← this file (Codex guidance; generated from CLAUDE.md)
├── README.md                       ← public product README (v1.0 downloads + v1.1 development notes)
├── LICENSE                         ← USACE-RMC notice, conditions, and disclaimer
├── global.json                     ← pins .NET 10 SDK (latestFeature roll-forward)
├── Directory.Build.props           ← RepositoryRoot + opt-in EnforceXmlDocumentation
├── Directory.Packages.props        ← central package versions (RMC.Numerics 2.*)
├── NuGet.config                    ← local feed C:\GIT\numerics\packages + nuget.org
├── docs/
│   ├── index.md, references.md    ← doc map + IEEE-numbered bibliography
│   ├── ROADMAP.md                  ← the phased roadmap (single source of truth for phases)
│   ├── PROGRESS.md                 ← session progress log — update every session
│   ├── verification.md             ← oracle-conversion strategy + tolerance policy
│   ├── REMAINING-WORK.md           ← the live map of remaining v1.1 work + recorded scope rulings
│   ├── requirements/               ← normative specs (architecture + shared-functions strategy)
│   ├── technical-reference/        ← per-family math docs (grows per phase)
│   └── verification-requests/      ← user-executed reference-run specs (as needed)
├── examples/                       ← v1.0 example projects (.tra) + future headless examples
├── scripts/
│   ├── validate-code-xml-docs.ps1  ← doc coverage + namespace/culture/dependency/traceability guards
│   ├── validate-verification-traceability.ps1 ← legacy/report scenario reconciliation
│   ├── sync-agents-md.py           ← regenerates AGENTS.md from CLAUDE.md
│   └── perf/                       ← PerfHarness (Stopwatch console runner, NOT in the .sln)
│       └── RESULTS.md              ← per-commit measurements + the byte-gate hashes
└── src/
    ├── RMC.TotalRisk/              ← the model library
    │   ├── Core/                   ← kernel: bases, hashing, seeding, serialization helpers
    │   │   ├── Enums/              ← every enum, one per file
    │   │   └── Interfaces/         ← every interface
    │   ├── RiskFunctions/          ← factory + Hazards/ Transforms/ Responses/ Consequences/
    │   ├── Systems/                ← Components/ and Components/Graph/
    │   ├── Analyses/               ← RiskAnalysis, CostBenefitAnalysis (Phase 4+)
    │   └── Results/                ← results containers (Phase 4)
    ├── RMC.TotalRisk.Tests/        ← fast unit tests (folders mirror the library)
    └── RMC.TotalRisk.Verification/ ← Monte Carlo verification tests
```

## Roadmap and Priorities

The phased roadmap lives in `docs/ROADMAP.md` — one phase per working session, exit gates listed there. Current status is tracked in `docs/PROGRESS.md` (newest first; update every session).

**Porting sources (in order of authority):**
1. `C:\GIT\RMC-TotalRisk-Dev\RMC-TotalRisk\RMC.TotalRisk.IO\Project\Elements\` — the partial VB→C# port of the input-function clusters and engine. Primary source for class shapes and math bodies.
2. `C:\GIT\RMC-TotalRisk-Dev\RMC-TotalRisk\RMC.TotalRisk\` — the legacy VB.NET v1.0 engine. Cross-check when the C# port is ambiguous or incomplete.
3. `C:\GIT\RMC-TotalRisk-Dev\RMC-TotalRisk\Test_TotalRisk\` — ~128 hand-rolled Monte Carlo oracle methods (fixed seeds, no asserts). Source for verification tests, NOT for model code.
4. Normative design specs: `docs/requirements/MODEL_LIBRARY_ARCHITECTURE.md` (v0.6+) and `docs/requirements/SHARED_FUNCTIONS_STRATEGY.md`. Where the spec deliberately departs from legacy (seeding, sampling, no-BestFit imports), the spec wins.

**Porting rule (amended 2026-07-20):** preserve the v1.0 **API surface** (signatures, defaults, lifecycle) and its **reference results**, but actively improve the implementations — fix numerical fragility, imprecision, and inefficiency wherever v1.0 is demonstrably flawed (example: v1.0 `ForceMonotonic` nudged by an absolute machine epsilon that is unrepresentable at hazard magnitudes ≥ 1 and was swallowed by the tolerance-equal ordinate setter — v1.1 uses a scale-aware nudge). Every improvement is documented in the type's XML `<remarks>` and the session's PROGRESS entry, and covered by a test. Verification oracles (Phases 5–6) remain the arbiter that improvements did not change reference results beyond statistical tolerance. Larger deliberate behavioral changes (content-based seeding, LHS sampling, JSON results) are those ratified in the requirements docs.

## Ported Types Matrix

Status legend: — planned · P ported · T unit-tested · V verification coverage. Update as work lands.

| Cluster | Type | Status | Verification anchor |
|---|---|---|---|
| Core | IRiskFunction / RiskFunctionBase / CanonicalContentHasher / CanonicalizationRules / SeedHelpers | P/T | hash-invariance + seeding unit tests (Phase 1 — landed 2026-07-20) |
| RiskFunctions | RiskFunctionFactory | P/T | round-trip + cluster-filter unit tests (Phase 3 — landed 2026-07-20) |
| RiskFunctions | RiskFunctionResolver / IRiskFunctionResolver | P/T | reference round-trip + stale-id/lenient-name policy tests (Phase 3.5 — landed 2026-07-20) |
| Hazard | TabularHazard | P/T/V | NFIP assurance deterministic dense-tabulation variant vs exact quadrature (`NfipAssuranceVerification`; docs/verification/nfip-assurance.md); A10 (landed 2026-08-28): the `Extrapolation` policy — conditional-presence serialization, sampled-wrapper wiring incl. the mean-curve builders' internal `SampleCore`, the Error guard, and the widened inverse tails (`ExtrapolationVerification`) |
| Hazard | ParametricUnivariateHazard | P/T/V | NFIP exact-LP3 API + Table 104 pins + posterior-injection assurance + active TOL 60/65 bootstrap-hazard ensembles (`NfipAssuranceVerification`) |
| Transform | TabularTransform | P/T/V | NFIP rating-curve chain (log-flow interpolation) in every active assurance scenario (`NfipAssuranceVerification`); A10 (landed 2026-08-28): the `Extrapolation` policy — conditional presence, wrapper wiring, Error guard (`ExtrapolationVerification`) |
| Response | TabularResponse | P/T/V | joint/competing/common-cause/mutually-exclusive oracle families (Phase 5 — landed 2026-07-23; docs/verification/); A10 (landed 2026-08-28): the `Extrapolation` policy — conditional presence, [0,1]-clamped extension, policy-aware below-table warning, Error guard with the inverse hold (`ExtrapolationVerification`) |
| Response | ParametricResponse | P/T/V | posterior-injection realization-for-realization anchor (`SingleComponentUncertaintyVerification`, landed 2026-07-23) |
| Response | NonFailResponse | P/T/V | every Phase 5 engine scenario carries the non-failure mode (landed 2026-07-23) |
| Consequence | TabularConsequence | P/T/V | joint/competing/common-cause/mutually-exclusive + EAD oracle families (Phase 5 — landed 2026-07-23); A10 (landed 2026-08-28): the `Extrapolation` policy — conditional presence, zero-clamped extension, policy-aware below-table warning, Error guard (`ExtrapolationVerification`) |
| Consequence | ParametricConsequence + ClampedPowerFunction | P/T/V | function-level exact/lognormal/MC-oracle family (`ParametricConsequenceVerification`, landed 2026-07-21; docs/verification/parametric-consequence.md) |
| Consequence | CompositeConsequence + WeightedConsequenceFunction + CompositeUnivariateFunction | P/T/V | function-level 2024-report family (`CompositeConsequenceVerification`) plus day/night and bootstrap configurations behind the full engine (`CompositeEngineVerification`; docs/verification/composite-engine.md) |
| Hazard | CompositeHazard + WeightedHazardFunction | P/T/V | report Tables 44–46, exact mixture/competing-risks identities, index-parity uncertainty, and the legacy hazard configuration behind the full engine (`CompositeHazardVerification` + `CompositeEngineVerification`) |
| Response | CompositeResponse + WeightedResponseFunction | P/T/V | report fragility-axis mixture, exact weakest-link identities, index-parity bands, and the legacy response configuration behind the full engine (`CompositeResponseVerification` + `CompositeEngineVerification`) |
| Trees | TreeFragment / TreeNodeReference / response-branch contracts / TreeLinkMode / TreeDeletePolicy / ProbabilitySource + ProbabilitySourceKind (promoted 10B slice 0a) / lifted internal compilation machinery (ITreeComputeSource, TreePlanDependencies, ambient TreeCompilationScope, TreeCanonicalPaths, TreeMutationSnapshot, TreeDependencyCollector, IProjectedIdentityResponse) / TreeNodeImportance + Options/Result/Entry | P/T/V | immutable fragment/reference/branch-sample contracts + enum pins; fragment source IDs are authoring-only remap metadata, never serialized or hashed (Phase 10A, landed 2026-07-28); the bit-inert machinery lift and source promotion are byte-gate-proven (Phase 10B slices 0a/0b, landed 2026-07-31); node importance is the two-pass read-only MC sweep for BOTH tree kinds — content-derived per-pass MersenneTwister streams, shared-once fault draws, Numerics statistics, exact closed-form + independent-reimplementation oracles (`FaultTreeMonteCarloVerification`; docs/verification/fault-tree.md) |
| Response | EventTreeResponse + EventTree + EventTreeLinkNode + EventNodeBase / InitiatingNode / ChanceNode / RemainderNode / ProbabilitySource | P/T/V | the complete Phase 10A implementation: legacy path-product/remainder/normalization algebra; scalar/aligned-table/ordinary-response sources; controlled transactional authoring including fresh-ID copy/paste, replace, materialize, delete-materialize, and prune; deterministic expanded topological/unreachable/internal/external reference queries; internal/external `IndependentClone` occurrence expansion; direct and multi-level `EventTreeResponse` probability sources evaluated at caller hazards; exact recursive sampler discovery through nested responses/tables/links; independent repeated-occurrence streams; indexed/percentile/LHS propagation; resolver-backed two-mode XML and nested reference repair; complete structural/source/mixed cycle paths; transactional sampler rollback; projected recursive hash/seed identity; import-only conversion of the recursive v1.0 `Node` shape and shipped templates; and opt-in arbitrary n-way terminal graph outputs with append-only stable ports, ID-first/name-fallback persistence, projected per-leaf compute, exact stale-connection reject/cascade/materialize policy, and graph/tree rollback; plus an instance-scoped immutable compiled occurrence/evaluation plan with complete revision/event/fingerprint invalidation, separately checkpointed lazy branch preparation, primitive-index linear evaluation, exact cache rollback, concurrent read-only publication, and the hash-gated 1,105-instruction F5 fixture. Graph-connected per-leaf consequences are independently verified (`EventTreeVerification`, 14 tests; `docs/verification/event-tree.md`). Fixed-seed property generation (128 cases with bounded deterministic minimization), million-route branch Monte Carlo, 12-replicate aggregate SRS/LHS variance reduction, and production sequential/multi-worker/default scheduling close the remaining gates; unit-only coverage is 90.40% (12,257/13,584), the fast suite is 781/781, F5 retains one published plan and its byte gate, Phase 10A is complete, and Phase 10B is unblocked. |
| Response | FaultTreeResponse + FaultTree (+ authoring partial) + FaultTreeGateNode / FaultTreeBasicEventNode / FaultTreeHouseEventNode / FaultTreeTransferNode / FaultTreeGateType + internal ROBDD kernel (FaultTreeBdd / FrozenFaultTreeBdd / budget exception) + FaultTreeCutSet / FaultTreeCutSetEvent | P/T/V | the complete Phase 10B implementation: the single-parent authored tree whose expanded plan is the DAG, with ALL sharing through `SharedLogicalEvent` transfers and deliberate repetition through `IndependentClone`; exact And/Or/two-input-Xor/KOfN algebra through the memoized internal ROBDD frozen to allocation-free children-before-parents evaluation — the single exact evaluation path (the read-once fast path was deliberately not implemented); the loud runtime-only `BddNodeLimit` (default 1,000,000, never serialized/hashed); Rauzy minimal-cut-set inspection bounded by `GetMinimalCutSets(maxCutSets = 10000)` with the non-coherent (Xor) refusal — never the probability engine; scalar/aligned-table/referenced-response sources (tabular, event, fault) at arbitrary acyclic nesting with cross-kind cycle diagnostics; unified-variable sampler discovery (shared events sample once, clones fork content-seeded streams); the full EventTree-parity transactional authoring surface incl. fresh-ID copy/paste, replace, materialize (shared→independent conversion deliberately moves values — tested both directions), delete policies, prune, and exact rollback; two-mode XML on the `<Nodes>` + single-parent `<Input ParentNodeId ChildNodeId Order/>` shape whose reader unifies repeated self-contained embeds of one function (corpus-discovered, landed 2026-07-31); projected identity carrying `SharedVariable` first-occurrence ordinals; and the fixed-seed 128-case property corpus (seeds 0x10B20261–64) with an exhaustive Boolean-enumeration oracle and bounded minimizer. Verified by the greenfield family `FaultTreeVerification` (10) + `FaultTreeMonteCarloVerification` (7) — exhaustive enumeration, closed-form/bridge/cut-set/anti-approximation pins, referenced-source and transfer-expansion parity, graph-connected bit-equal risk equivalence, budget diagnostics, million-trial Boolean simulation under a Hoeffding bound, strata/variance-reduction/scheduling/invariance closure, and node-importance oracles (docs/verification/fault-tree.md). Unit-only coverage is 90.39%, the fast suite is 868/868, F7 pins the 23,344-node frozen diagram with one published plan and its byte gate, and Phase 10B is complete. A1 (landed 2026-08-28): the exact importance surface — `FaultTreeImportance.Compute` + Options/Result/Entry (Birnbaum/criticality/Fussell-Vesely/RAW/RRW via two allocation-free frozen-diagram conditional evaluations per unified variable at the mean or a co-monotonic percentile; runtime-only query, nothing serialized; coherent-only with the cut-set-style Xor refusal; reduced-out ⇒ Birnbaum exactly 0; P=0 ⇒ ratio NaN; P(0)=0 ⇒ RRW +∞), verified against the exhaustive enumeration extended with per-variable forced conditionals (`FaultTreeVerification` 11/11 isolated); closes the REMAINING-WORK future-work note |
| Transform | CompositeTransform + WeightedTransformFunction | P/T/V | greenfield, Average-only (Mixture deferred until the engine can enumerate transform branches); exact linear algebra + ω²-additive Normal theory (`CompositeTransformVerification`, landed 2026-07-25; docs/verification/composite-transform.md) |
| Core.Enums | FailureModeMethod / DependencyType / JointConsequenceType / RiskType / HazardDimension | P/T | value/order pinning tests (Phase 3 — landed 2026-07-20) |
| Core.Enums | HazardFunctionType / TransformFunctionType / ResponseFunctionType / ConsequenceFunctionType / RiskElementType / RiskAnalysisMode | P/T | member pinning + not-serialized assertions (v0.10 namespace reorganization — landed 2026-07-20) |
| Core.Enums | RiskSerializationMode | P/T | mode-invariant hashing + by-reference round-trip tests (Phase 3.5 — landed 2026-07-20) |
| Core.Enums | CompositeCombinationType (serialized canonical-hash content; Mixture vs CompetingRisks for the hazard/response composites) | P/T | member/value pins + default-is-Mixture pin (Phase 9 — landed 2026-07-25) |
| Core.Enums | RiskIntegrand (adaptive-refinement objective) / VegasTailFocusMode / SystemRiskType | P/T | member/order pins + options round-trip + hash-recipe tests (Phase 4 — landed 2026-07-22) |
| Core.Enums | SensitivityMeasure (runtime-only, never serialized) / RiskMeasure (member NAMES are append-only serialized contract since 2026-08-27 — `TolerableRiskCriterion` persists and echoes the measure by name; values stay runtime-only) | P/T | member/order pins (Phase 6.6 — landed 2026-07-24); criterion name-serialization round trip (2026-08-27); A3 (landed 2026-08-27): the given-data members `FirstOrderSobol`/`PawnMedian`/`BorgonovoDelta` appended (values 3–5) — `Correlate` routes them through the upstream `GlobalSensitivity` estimators under the shared 20-bin `GivenDataBins` convention, needing ≥ bins valid pairs (null below), non-finite samples coercing to zero per the v1.0 convention; pinned bit-exact against direct upstream calls and the linear-map closed binned form (`SensitivityVerification` 4/4) |
| Core.Enums | BranchPolarity (serialized canonical-hash content; values double as response output-port indices) | P/T | member/value pins + stage polarity round-trip (Phase 6.7 — landed 2026-07-24) |
| Core.Enums | SamplingScheme + the 6b Sobol surfaces (RULED attempt, landed 2026-08-28): `ScrambledSobol = 3` appended (serialized by name; selecting is hashed compute content; scheme switches never re-roll stream seeds — pinned) generated by `SeedHelpers.ScrambledSobol` (one joint sequence per function, seeded from the SAME content-derived positive stream seed) at all three matrix generators (`RiskFunctionBase.SetupSampler`, the `FailureMode` coupling matrix, the `CompositeConsequence` mixture selector); Validate advises on non-power-of-two counts; upstream `Vegas.SobolSeed` (numerics `fa91884`, rides 2.2.0) + the conditional-presence `RiskAnalysisOptions.UseSobolJointSampling` restore the v1.0 quasi-random joint driver under the content-seed contract (deliberate hashed movement when enabled; default false byte-inert) | P/T/V | member pins + conditional-presence/hash tests + walk/coupling/mixture determinism + seed-inertness + the joint-driver movement/reproducibility; `SystemRiskVerification` 6/6 isolated (Sobol-driver oracle parity + γ-audit + byte reproducibility); `LhsVarianceReductionVerification` extended (Sobol-vs-MC variance floor, mean-only reproduction, rename bit-inertness) |
| Core.Enums | ExtrapolationPolicy (serialized by NAME only when non-default on the five tabular/nonparametric function types — conditional presence keeps every existing form/hash/seed byte-identical; a configured policy is hashed compute content; `Error` maps to the upstream `ExtrapolationSides.None` with the range check TotalRisk-side via the internal `RangeGuarded*` wrappers + `EvaluationFaultScope`) | P/T/V | member/value pins + the mapping pin (A10 — landed 2026-08-28); per-type conditional-presence/hash/wiring/guard tests; `ExtrapolationVerification` (5, isolated) incl. the engine-scale byte-identity and Error-surfacing pins |
| Systems | ResponseStage / FailureMode | P/T/V | joint/competing/common-cause/mutually-exclusive oracle families across the dependency × combination matrix (Phase 5 — landed 2026-07-23); Phase 6.7 (landed 2026-07-24): `ResponseStage.BranchPolarity` (resolved-on-write — THE deliberate hash event), multi-stage polarity-product compute, runtime-only `ProjectedResponseOrdinals`/`ProjectedTerminalName` projection stamps (`CascadeEndStateVerification`, docs/verification/cascade-end-states.md); Phase 11 S5 (landed 2026-08-05): the serialized `FailureMode.SecondaryHazardToResponse` chain written ONLY when non-empty (the conditional-presence guarantee — every pre-bivariate mode's form, hash, and seeds bit-identical, pinned by test), sampler walk appending the chain AFTER `ResponseToConsequence` (existing ordinals proven unchanged via seed-scribe capture vs independently constructed expectations), `IsDeterministic` including the chain, the reserved gates replaced by the parent-typed bivariate matrix (univariate/unparented parent ⇒ Secondary binding/dimension/chain + bivariate transform/consequence-in-mode Errors BUT a bivariate RESPONSE legal — collapse mode incl. cascade stages; bivariate parent ⇒ both bindings legal, dimension-scoped position bounds — Secondary requires an explicit position ∈ [0, chain length] — joint response single-stage + `SecondaryLevelCount` ≥ 2 Errors, Y-chain entries validating like trailing transforms with the bivariate-inside-chain Error, trailing-transforms-under-bivariate-consequence Error), bound-marginal label continuity (the secondary axis walks from the bivariate hazard's declared secondary pair; Warnings only), and the consequence input routing contract documented for the S6 engine; `ResponseStage` unchanged |
| Systems | EndStateGroupLayout (leaf-signature state groups: exclusive partitions, Q2 standalone ejection, final-polarity classification, sibling pairing, trivial-layout detection) | P/T/V | layout structural matrix + the cascade family's across-unit scenarios (Phase 6.7 — landed 2026-07-24) |
| Systems | SystemComponent (graph-owned; projection + identity hash + occurrence indices + MVN; Phase 6.6 `ProfileHazardElementId` seed-inert profile selector + `CollectSensitivityInputs` shared walk) | P/T/V | engine scenarios + seed-bug regressions (Phases 4–5) + the multi-component system matrix incl. identical-content occurrence-index scenarios and the `HazardThreshold` API surface (Phase 6 — landed 2026-07-23); profile remap + hash-inertness (`RiskProfileVerification`, Phase 6.6 — landed 2026-07-24); Phase 11 S5 (landed 2026-08-05): projection stamping — `HazardBinding` from the root exit port (port 1 ⇒ Secondary; existing models always port 0 ⇒ zero serialized movement), the path's secondary chain collected through `TryResolveSecondaryChain` into `mode.SecondaryHazardToResponse` (non-throwing; unresolvable ⇒ empty), binding generalization (hazard port 1 = raw-Y position 0; a secondary-chain transform ⇒ Secondary position k+1); the profile selector's port-0 first-hazard-edge guard; `CollectSensitivityInputs` enumerating `MarginalX`/`MarginalY` (a bivariate hazard itself contributes no column) and the secondary chain after trailing transforms; `AddFailureMode` refusing bivariate topology with `NotSupportedException` (a linear expansion cannot wire secondary connections); `AssignOccurrenceIndices` unchanged; hazard stays sampler ordinal 0; Phase 11 S6 (landed 2026-08-06): `EstimateRecordedFailureEntries` ×(bins+1) under a bivariate hazard (the joint-entry limits price the (bin × pathway × branch) cross product) + the decision-10 competing guard — multi-unit `CompetingFailures` over a bivariate hazard requires every failure state's SRP primary-pure (Primary binding, no joint response, no bivariate stage transform): Validate Error, mirrored by a loud `SampledComponent` ctor throw before the incidence pre-processing |
| Graph | IRiskElement / RiskElementBase / Hazard-Transform-Response-ConsequenceElement / RiskConnection / ComponentGraph / factory / resolver / HazardSourceOption | P/T | levee projection acceptance + identity-inertness unit tests (Phase 3); engine scenarios (Phases 5–6); Phase 11 S5 (landed 2026-08-05): the two-port graph surface — arity-derived ports on all four element types (hazard out 2, transform in/out 2 with port 0 = z and port 1 = the secondary passthrough, response in 2 with outputs unchanged, consequence in 2 when any entry is bivariate); `SecondaryInput` on `TransformElement`/`ConsequenceElement` (the `ResponseElement` pattern verbatim: the `SecondarySource*` triple, Input-first `GetInputConnections` keeping secondary edges off-path, 2/3-slot snapshot-rollback + branch-detach widening); the shared public `TryResolveSecondaryChain` (univariate front-inserts, bivariate port-1 passthrough hops under the per-segment directness guard — no univariate transforms between bivariate elements — closed loud error set, visited-set cycle guard) consumed by projection, validation, and the picker; the mode-dependent wiring matrix (joint mode requires the wired secondary on single-response paths; collapse mode legal only with a null secondary under a univariate root; bivariate transform/consequence under a univariate root Errors; primary-kind input tracing; the one-secondary-chain-per-path identity Error; the unused-port-1 Warning); generalized hazard-source bindings (raw Y and secondary-chain targets legal, a bivariate transform's passthrough rejected); the picker's port-1 option labeled from the secondary pair plus chain options at position k+1 (`HazardSourceOption` record unchanged); `GetReferencedFunctions` yielding a bivariate hazard's marginals through the same seen set |
| Results | SampledComponent / SampledFailureMode / ComponentRiskOutput / RiskComputeFlags / Curve / Curves / RiskPoint / realization + summary trees / EnsembleResults (JSON) | P/T/V | Phase 4 (landed 2026-07-22): combination-rule known-point tests, exact-LEC/Welford/measure tests, JSON round-trips; engine families `EngineReproducibilityVerification` + `ExactLecTailVerification` (docs/verification/); Phase 6.5 (landed 2026-07-23): the multi-consequence axis — per-type `AdditionalCurves` + extents on the realization trees, `ConsequenceResults`/`AdditionalConsequences` + declared-label echo on the summary tree, `Curves.GetCurve(RiskType)` (`MultiConsequenceVerification`, docs/verification/multi-consequence.md); Phase 6.6 (landed 2026-07-24): the profile catalog (`CumulativeFailureProbabilities`/`CumulativeExpectedConsequences`/AEP-axis `SystemResponseProbabilities` + `[JsonIgnore]` normalized views), `RiskContribution` + `ContributionAccumulator` (Shapley + consequence-proportional splits at FM/component/system scope), `EnsembleSummary` + `ConvergenceDiagnostics` (`EnsembleResults.Summary`), `SensitivityResults`/`SensitivityEntry` (unpersisted) — families `RiskProfile`/`Contribution`/`ScalarUncertainty`/`Sensitivity` + docs/verification pages; Phase 8.5 (landed 2026-07-26): `QuadratureMassLedger` + `Curve.ApplyRecordedMass` replacing the midpoint-trapezoid mass partition (N7 adoption), `ContributionAccumulator.FinalizeFromLedger`, and the `RiskMeasureOptions` gates on the optional measures; Phase 8.6: lazy dependent failure-mode enumeration for every dependency plus deterministic remaining-budget clipping before all downstream consumers; Phase 11 S6 (landed 2026-08-06): the conditional-bin engine — `SampledComponent.ComputeRisk` gains one optional `hazardNonExceedance` parameter (NaN ⇒ derive from the sampled primary marginal) and ONE `_conditionalHazard == null` branch selecting the unchanged univariate body verbatim (F1 byte gate bit-exact — the zero-overhead proof); the bivariate body fills the S2 trapezoid bins once per X, evaluates EVERY mode at the same (x, y_j), runs the existing combination kernels per bin (combine-then-marginalize, decision D.3 — the joint pathway probabilities pre-scale by w_j so the fold flows exactly through entries, expected sums, and Shapley splits), and folds into ONE risk point per stream per evaluation with contribution samples submitted once per (X, type); `SampledFailureMode` captures the projection stamps, the sampled secondary chain, and per-realization `Bilinear` wrappers (bivariate stage/trailing transforms at (signal, y′), the joint response surface clamped [0,1] after back-transform, per-type bivariate consequence surfaces) with ctor-computed `IsBinInvariant`/`IsSrpBinInvariant`, the staged `BeginBinnedEvaluation`/`ComputeRiskBinned`/`CommitBinnedPoint` members, public `SRPAt`/`ConsequenceInputAt` (joint-mode `SRP(x)` stays total, returning the stored v1.0 collapse), loud guards on the univariate members, and the extended `InverseSRP` guard; no draws anywhere in the bin loop (decision 8, pinned by counting doubles); recording evaluations allocate only the lists the committed points adopt; A4 (landed 2026-08-27): the epistemic realization-weight surface — `EnsembleResults.RealizationWeights` (the single authoritative RAW vector, reliability semantics; null-suppressed append-only JSON, so unweighted payloads are byte-identical — pinned by the captured pre-weight SHA-256), `SetRealizationWeights` eager validation vs the lenient serialization property (the shared rule: length = slot count, finite, ≥ 0, not all zero), weighted `EnsembleSummary` (weighted mean + symmetric weighted percentiles via the upstream vector-k call with pairwise NaN/weight filtering, indicators at √(V/N_eff) with `EffectiveRealizationCount` = Kish (Σw)²/Σw², effort aggregates deliberately raw; the null gate selects the existing code verbatim; unit weights bit-exact only where the plotting positions i/(n−1) are representable — documented), `AnalysisRunManifest.RealizationWeightsHash` (run-input fingerprint only, trailing optional ctor param, schema stays 1, post-hoc weights never touch the manifest), and the ruled load-integrity behavior — invalid stored weights never throw: results cleared, manifest kept, `LoadDiagnostics` populated, the restored analysis unestimated; A7 (landed 2026-08-27): `TolerableRiskConfidence` — the epistemic P(measure > threshold) entries on `EnsembleSummary.TolerableRiskConfidence` (criterion echo by enum name + the weighted strict-exceedance fraction; null-suppressed, absent without criteria) (`WeightedEnsembleVerification`, docs/verification/weighted-ensemble.md); B2 (landed 2026-08-27): the unpersisted value-of-information containers — `ValueOfInformationResults`/`ValueOfInformationEntry`/`ValueOfInformationGroup`/`TolerableRiskConfidenceMovement` (plain query results, never serialized; NaN-last stable ranked views) (`ValueOfInformationVerification`, docs/verification/value-of-information.md) |
| Analyses | RiskAnalysis + RiskAnalysisOptions + AnalysisBase/IAnalysis (1D engine core + Phase 4b multi-D system risk + Phase 4c reliability mode + Phase 6.5 declared consequence-type axis: `ConsequenceTypeDescriptor` declaration, strict bubble-down validation, every declared type computed per pass) | P/T/V | Phases 4–6 (landed 2026-07-22/23): seed-bug regressions (`EngineReproducibilityVerification`), mean parity (`SingleComponentMeanParityVerification`), MC tail oracles (`ExactLecTailVerification`, `SystemRiskVerification` incl. the γ-audit), the five Bucket-1 families (`JointFailures`/`CompetingFailures`/`CommonCause`/`MutuallyExclusive`/`Ead`), `SingleComponentUncertainty` + `CombinationMethodConsistency`, and the Phase 6 families — `SystemRiskMatrix` (legacy `Test_MC_SystemRisk`, report tables 77–103 pinned), `RiskAnalysisCombos`, `NfipAssurance` (reliability mode + Table 104 + the full-uncertainty assurance ensemble), `LhsVarianceReduction`; Phase 6.5 (landed 2026-07-23): `MultiConsequenceVerification` (two-type oracle + bit-identity + system identities); Phase 6.6 (landed 2026-07-24): the unified sensitivity engine (`MeasureSensitivity`/`MeasureSensitivityMatrix`/`HazardLevelSensitivity` — ratified amendment: no legacy `Sensitivity()`/`RiskAtHazardLevel` port), per-type `ConsequenceThreshold` plumbing, contribution aggregation (additive Poisson-binomial φ DP + joint accumulators), `SamplerSeedMap`/`SeedScribe` + `CapturedSamplerSeeds`/`PinnedSamplerSeeds` (§5.5.8 seed-stable perturbation mode) — the four new families, each run isolated; Phase 6.7 (landed 2026-07-24): the Q-X closure — multi-stage acceptance, the `EndStateGroupLayout` combination-unit kernels with the claimed-complement mixture, the narrow competing gate, and end-state labels (`CascadeEndStateVerification` run isolated; docs/verification/cascade-end-states.md); Phase 11 S6 (landed 2026-08-06): the bivariate call-site plumbing — the 1D `Evaluate` objective and the AFP probe pass their true u as `hazardNonExceedance`, the joint VEGAS integrand passes its clamped local probability (its probability argument stays the recorded weight), sensitivity leaves NaN; NOTHING else moves — the AGK objective returns one marginalized scalar per X, both mass-ledger adoption gates hold (one point per X + exact Σw = 1), endpoint rectangles and `BuildHazardBins` stay on the primary support, the AFP probe returns the y-marginalized failure probability (the correct VEGAS γ target unmodified), every `RiskIntegrand` member operates on the marginalized output (TailConditionalRisk's α boundary is on the primary axis), tolerance discipline untouched, and `RiskAnalysisOptions` gains NO field (bins live on the hazard and enter identity through its hash); A4 (landed 2026-08-27): `RiskAnalysis.RealizationWeights` — the runtime-only run-input weight vector (public `IReadOnlyList<double>?` with defensive copy + view; never serialized, never hashed, never seed-affecting; the setter invalidates `IsEstimated`; `Validate()` Errors for mean-only-plus-weights and the shared weight rule, with a race-safe re-check inside the run), snapshot-threaded through `PostProcessUncertainty` into the weight-aware `RiskPercentileAssembler` (LEC/profile/system-response band curves and the curve-scalar slots; null = the existing path verbatim), stamped into the published ensemble and fingerprinted in the manifest; weights never move a sampled realization (per-index bit-invariance pinned at unit and engine scale), `RiskAnalysisOptions` again gains no field, and the results-injection ctor restores `IsEstimated` only over a non-empty ensemble (the cleared-on-load shape reports unestimated); A7 (landed 2026-08-27, the ratified serialized-criteria hash event): `RiskAnalysisOptions.TolerableRiskCriteria` — immutable `TolerableRiskCriterion` entries (measure/stream by enum NAME, consequence-type position, finite threshold; the parameterless ctor is the 1e-3 mean-Excess guideline) serialized as a CONDITIONAL child so every criteria-free options form and hash is unchanged (pinned) while configured criteria are hashed compute-relevant content that never touches a seed (per-realization bit-invariance pinned at verification grade); the engine evaluates system-scope strict-exceedance weight fractions through the existing `SelectScope`/`ExtractMeasure` machinery into the summary block, `ComputeTolerableRiskConfidence()` re-evaluates post-hoc over the stored ensemble incl. post-run weights, Validate gains the indexed criterion Errors + the type-position bound + the mean-only Warning, and NO `RiskMeasureOptions` flag exists — the feature gates on criteria presence, so the recorded `"All"` serialization trap never engages; B2 (landed 2026-08-27): `RiskAnalysis.MeasureValueOfInformation` — the weighted given-data value-of-information query over the recorded `SensitivityInput` columns (which now carry `GroupLabel`, the owning-function study key): equal-weight-bin conditional variance per input through the internal `ValueOfInformationEstimator` (exact between+within decomposition; documented 1/B² discretization and B/n noise biases; the 20-bin convention), Σ-main-effects function rollups (exact under additivity, conservative otherwise), the perfect-information total variance, and per-criterion `TolerableRiskConfidenceMovement` blocks at the system scope (baseline bit-equal to the published confidence, the 2p(1−p) ceiling, per-entry expected movement); runtime-only — no options field, nothing serialized or hashed, no seed movement, all eight byte gates bit-exact (`ValueOfInformationVerification` — the linear-Gaussian closed-form + probit-quadrature oracles and the independent re-implementation over public summaries, unweighted and weighted, run isolated); A9 (landed 2026-08-28): the runtime-only retention surface — `RetainRealizations` keeps the full per-realization ensemble on `RetainedRealizations` and enables `ReassemblePercentileBands(weights?)` (the weight-aware assembly re-run over retained state; the post-hoc weighted re-band is BYTE-IDENTICAL to a run carrying those weights as input — the A9+A4 synergy, pinned), `RetainedIntegrationDetailIndex` (−1 mean pass / ensemble index) skips the selected realization's `DumpMemory` so `RetainedIntegrationDetail` carries the recorded risk-point ledger on its COMPONENT curves (system curves are Clone()s that exclude points by design); run-start snapshots, publish/clear lifecycle, Validate memory-cost + no-match Warnings; nothing serialized/hashed/seed-affecting, default-off byte-inert (pinned at test and gate scale); A2 (landed 2026-08-28): `EstimateSecondaryDiscretizationError(componentIndex)` — the a-posteriori secondary-bin Richardson diagnostic: the mean pass integrated at the configured/halved/quartered counts through internal diagnostic snapshots (`BivariateHazard.SampleBivariateAt(bins)` over the SHARED grid derivation + `SystemComponent.SampleWithConditionalBins` + the `SampledComponent` conditional-override ctor slot — stored count/hash/seeds untouched), AFP + per-type Excess-mean `DiscretizationEstimate`s (extrapolated limit, estimated relative error, observed convergence ratio = the regime check; `SecondaryDiscretizationDiagnostic` unpersisted), configured level BIT-EQUAL to a mean-only run and the reduced levels BIT-EQUAL to models configured at those counts (pinned); needs ≥ 12 bins; oracled against the convergence study's measured true errors incl. the pre-asymptotic regime flag (`CopulaDependenceVerification` 7/7); A6 (landed 2026-08-28): the epistemic conditioning surface — runtime-only `RiskAnalysis.FractilePins` (`FractilePin(functionId, percentile)`; setter invalidates `IsEstimated`; never serialized/hashed/seed-affecting) applied at the sampler walk by overwriting the target's own percentile matrix AFTER seeding (`RiskFunctionBase.OverrideSampledPercentiles`; the walk records applied ids and the run refuses unapplied conditioning pins), Validate matrix (duplicate/no-match Errors; deterministic/no-surface/consequence no-effect Warnings — coupling-column pinning recorded future work; mean-only Warning + byte-inert ignore), the three diagnostic re-seeds apply current pins, the constant-column convention (a bit-constant input reports exactly 0 in the given-data measures and resolves 0 VoI variance — the tie-order noise floor short-circuited), and the exact Median-LHS recovery identity (`SensitivityVerification` 5/5 isolated); A5 (landed 2026-08-28): `MeasureExposurePeriodRisk(periodYears, discountRate, riskType, scope…)` → unpersisted `ExposurePeriodRiskResults`/`ExposurePeriodInterval` — the ruled binomial P_T = 1−(1−p)^T primary convention (log-space `Tools.Log1p`/`Expm1` evaluation; the Poisson rate form documented at ≈ p·(T−1)/2 relative difference), cumulative/discounted/equivalent-annual expected consequences (the annuity round trip reproducing the annual mean — the stationary identity; the base-vs-future `PlanRow.EquivalentAnnual` composition stays CBA-layer), weighted-aware from birth over the stored `RealizationWeights` incl. post-hoc, stationarity caveat documented; ruled fast-only (exact closed-form identities — independent `Math.Pow` forms, the bit-exact undiscounted PV≡cumulative pin, upstream weighted-reducer parity; no verification family, the A9 precedent); A8-certificate (landed 2026-08-28, RULED runtime-only): `RiskAnalysis.JointCertificate` (`JointConvergenceCertificate`, never serialized, null off the joint method) — the RECORDING-pass per-dof χ² and relative SE the integrator recomputes after `Initialize = 1` resets its accumulators (the stored per-realization `ChiSquared` is the WARM-UP pass's, now documented), captured index-owned at the joint call site into ensemble mean/max aggregates + mean-pass slots on mean-only runs (a full run assembles its published mean from percentiles and integrates no −1 pass — the slots are NaN there by contract, pinned) |
| Analyses | SystemConvolution (exact lattice FFT convolution — the additive system aggregation kernel) | P/T/V | Phase 4b (landed 2026-07-23): exact-enumeration/mean-preservation/variance-additivity unit tests; system-LEC-vs-event-oracle family (`SystemRiskVerification`); the Phase 6 system matrix runs it against every Independent legacy group at 65,536 nodes (`SystemRiskMatrixVerification`) ; **stays local by Phase 8.5 audit** — N8's `ConvolveDiscrete` is pairwise and re-derives its lattice step per fold, so an N-way fold through it re-bins and smears each atom once per fold |
| Verification | Legacy Dev suite + 2024 report scenario matrix | V | 142 source test methods classified (including event-tree `TestIO`); 116 applicable methods mapped to current tests (the two `Test_BivariateRisk.vb` oracles and the re-anchored `Test_DAMRAE.vb` scenario re-classified Covered 2026-08-07); all 49 report system/joint-failure scenarios covered; FDA/NFIP explicitly obsolete (`legacy-traceability.csv`, repository validator) |
| Analyses | CostBenefitAnalysis | — | future phase (the Phase 4 self-contained-analysis foundation is its design driver) |
| Transform | LinearTransform | P/T/V | Numerics `Test_Functions` closed-form anchors + the engine-chain ensemble vs a flat MC oracle + D=0 dense-quadrature parity (`ClosedFormFunctionsVerification`, landed 2026-07-25; docs/verification/closed-form-functions.md; no legacy oracles exist — Dev-repo sweep) |
| Transform | PowerTransform | P/T/V | forward + IsInverse closed-form anchors, engine-chain ensemble vs MC oracle (`ClosedFormFunctionsVerification`, landed 2026-07-25; Numerics `PowerFunction.Minimum` derives from ξ — the wrapper keeps `Minimum` as API/hash surface only) |
| Hazard | NonparametricHazard | P/T/V | SF-8 vs HEC-FDA Table 38 (all 20 published log10 quantile pins) + the independent Brent-pipeline re-derivation (optimization-equivalence anchor) + reliability AFP oracles with bit pins (`ClosedFormFunctionsVerification`, landed 2026-07-25); A10 (landed 2026-08-28): the sampling-only `Extrapolation` policy (setter never re-derives — distinct from the serialized derivation-time `ExtrapolationEP` extension; conditional presence, wrapper wiring incl. the mean builder's `SampleCore`, Error guard; `ExtrapolationVerification`) |
| Hazard | BivariateHazard + IBivariateHazardFunction + SampledBivariateHazard | P/T/V | `BivariateRiskVerification` (legacy `Test_Bivariate_Risk` oracle at 1M/seed 45678, the exact union-grid SRP closed form + re-captured legacy 100M cross-check, the collapse-vs-joint cross-anchor) + `CopulaDependenceVerification` (independence exactness, Normal/Clayton analytic anchors, the three-fixture bin-convergence study pinning the discretization allowances — measured: smooth O(N⁻²) ratios 24.995/99.999; legacy seismic fixture 0.353/0.049/0.00198 rel at bins {20,100,1000}, the Φ⁻¹ tail-concentration mechanism documented — posterior-injected marginal realization parity at 0.1%) + 4 bivariate `EngineReproducibilityVerification` pins (docs/verification/bivariate-risk.md, copula-dependence.md; landed 2026-08-07). The Gumbel engine-level orientation pin is blocked by the Numerics `GumbelCopula.InverseConditionalCDF` Brent-bracket defect (ruling pending). Phase 11 S2 (landed 2026-08-03): marginal X/Y LINKS by `IRiskFunction.Id` (lenient name fallback, resolver repair, both serialization modes — the tree `FunctionReference` precedent); user-set Numerics copula defaulting to `IndependenceCopula` with fixed parameters (missing `<Copula>` reads as Independence); `SecondaryIntegrationBins` default 20, Validate-gated [3, 1000]; projected-identity hash (marginal CONTENT + copula type/parameters + bins as named X/Y attributes — mode/id/name/label-inert, X↔Y asymmetric); composite-forward seeding (one component ordinal; `HashCombine(seed, marginal.CanonicalHash(), 0/1)`, D=0); `SampledBivariateHazard.FillConditionalBins` — N+1 conditional-trapezoid nodes clamped [1e-16, 1−1e-16], last weight the exact residual (ordered Σw = 1 bit-exact), caller-owned buffers; the 9-rule Validate matrix incl. the univariate-marginal (nested-copula) guard and the `CompositeHazard` bivariate-child scope guard; 51 unit tests incl. exact child-seed and component-level mode-invariance pins; Phase 11 S6 (landed 2026-08-06): the mean `SampleBivariate()` overload (the mean Y marginal + cloned copula over the shared node vectors, the same SetupSampler gate — the engine's mean pass and deterministic probes consume it), engine integration complete (a bivariate component is ONE VEGAS dimension; `EstimateRecordedFailureEntries` ×(bins+1); the decision-10 multi-unit competing guard requires every failure state SRP-primary-pure, Validate Error + a loud `SampledComponent` ctor mirror). V arrives with `BivariateRiskVerification`/`CopulaDependenceVerification` (S7) |
| Transform | BivariateTransform + IBivariateTransformFunction | P/T/V | `BivariateRiskVerification.Test_Damrae_ChainedBilinear` — two chained bivariate transforms (deformation verbatim; the warning-time grid verbatim on an affinely def-remapped axis) inside the re-anchored PFM-08 oracle at 1M, engine at 1000 + 20 bins (landed 2026-08-07). Phase 11 S3 (landed 2026-08-04): deterministic two-way table z = f(x, y) on Numerics `Bilinear` (namespace `Numerics.Data`; convention `z[i,j] = z(x1[i], x2[j])`) with per-axis `HazardTransform`/`SecondaryHazardTransform`/`TransformTransform`; the pinned native extrapolation policy (both coordinates out ⇒ exact nearest-corner cell; one out ⇒ edge row/column clamp with 1-D linear interpolation along the in-range axis) documented loudly; the pinned interpolator thread discipline — `CreateInterpolator()` builds one fresh configured instance per sampled realization sharing the function's arrays (never per call, never shared, nothing cached; `Bilinear`'s mutable search state is the race); the 1-arg sampling trio throws `NotSupportedException` naming `Evaluate(x, y)`; deterministic D = 0; strict-ascending/finite/dims/cell validation + log and normal-Z guards per axis and output (normal-Z endpoints 0/1 legal — 0 clamps finite, 1 ⇒ +∞ in z-space, native legacy behavior); shape-preserving `<X1Values>/<X2Values>` pipe-G17 + `<ZValues><Row>` serialization (ragged payloads reconstruct as parsed with NaN fill and fail Validate — never truncated); default leaf-path hash (secondary labels strip via the S2 rules; transposing the surface moves it); `CompositeTransform` dual scope guard (Validate Error + usability rejection, domain-intersection skip). 29 unit tests. V arrives with the S6 engine + `BivariateRiskVerification` (the re-anchored DAMRAE chained-bilinear oracle) |
| Consequence | BivariateConsequence + IBivariateConsequenceFunction | P/T/V | `BivariateRiskVerification.Test_Damrae_ChainedBilinear` — the verbatim 20×5 life-loss surface as the bivariate consequence on (warning time, pool) behind the full engine vs the 1M oracle (landed 2026-08-07). Phase 11 S3 (landed 2026-08-04): the identical grid design with cluster names — output enum `ConsequenceTransform`, output labels via inherited `SpecifiedConsequence`/`ConsequenceUnit`; negative surface cells an advisory Warning; `CountExposureBranches()` = 1 with both `SampleExposureBranches` overloads throwing like the 1-arg trio; `CompositeConsequence` dual scope guard. 22 unit tests; shared plumbing lives in internal `BivariateTableSupport` so the two tables cannot drift. V arrives with the S6 engine (S6–S7) |
| Response | BivariateResponse + IBivariateResponseFunction + WeightedHazardLevel | P/T/V | `BivariateRiskVerification` — the joint 4×6 surface behind the legacy 1M oracle, the v1.0 arrangement measurement (stage-primary with PGA collapsed through auto-derived Voronoi weights lands 0.241% from the exact reference vs 35.4% for the default-bin bivariate arrangement of the same scenario — the engine integrates the primary axis exactly and discretizes the secondary, so the collapse mode is the better-conditioned arrangement when the secondary is mass-concentrated; pinned with a >50× head-to-head assert), AND the decision-12 collapse-vs-joint consistency cross-anchor (collapse distance to the exact dense reference 27.3% → 1.91% under interpolant-preserving both-axis refinement while the joint path closes 35.4% → 0.199%; engine collapse semantics pinned against an independent re-implementation at 5.1e-6) (landed 2026-08-07). Phase 11 S4 (landed 2026-08-04): the v1.0 port with the collapse mode fully preserved — SRP(xᵢ) = Σⱼ P[i,j]·wⱼ into `OrderedPairedData(xStrict, Ascending, yStrict:false, None)` wrapped by `EmpiricalDistribution` carrying `HazardTransform`/`ProbabilityTransform`, percentile/index overloads returning the mean collapse (D = 0), first/last-ordinate `Min/MaxProbability` semantics, `IsMonotonic` on the collapsed curve, and the legacy grid auto-resize on level Add/Remove (overlap preserved, zero fill; Reset deliberately inert) — plus the ratified decision-12 refinements: the stored `SecondaryHazardFunction` link persisted as TWO metadata attributes (`SecondaryHazardFunctionId`/`SecondaryHazardFunctionName`) repaired through the new never-throwing `IRiskFunctionResolver.TryResolve` (id-first, lenient name fallback, miss ⇒ null with the pending pair re-written verbatim so resolver-less round trips stay bit-equal — NEVER an Error; the loud `Resolve` stays the compute-link path); serialized weights as the authoritative compute content with derivation ONLY on explicit `EstimateWeights()` (no-arg + link-storing explicit overloads, all guards before mutation, single level ⇒ 1, Voronoi midpoints against the mean marginal CDF, last-bin residual absorption both directions with Σw = 1 exact — the > 1 branch is v1.0 defense unreachable without an exact rounding tie, pinned by a constructed Uniform below-one fixture) — the v1.0 load-order overwrite bug does not port; staleness as Validate Warnings (derived-vs-stored beyond 1e-8; unresolved/invalid link in automatic mode); the joint augmentation — secondary label pair, compute-relevant `SecondaryHazardTransform`, `SurfaceProbability`/`CreateInterpolator` on `Bilinear` (grid array shared, axis vectors copied from the parity collections per call) with the pinned extrapolation policy PLUS the [0,1] clamp after back-transform; the BRF-derived Validate matrix (≥2 primary/≥1 secondary — the ≥2-for-joint rule is mode-side at S5, p ∈ [0,1] cells, weight range + 1e-8 sum tolerance, per-axis log/normal-Z guards, exact v1.0 zero-row + monotonicity Warnings, single-secondary-level degeneracy Warning, bivariate-link Error); the v1.1 attribute envelope over the LEGACY child names (`PrimaryHazardLevels` pipe G17, `WeightedHazardLevel Level Weight` children, `ProbabilityValues`/`Probability_Row` rows — the future .tra importer lifts legacy payloads verbatim; shape-preserving ragged reads); default leaf-path hash with the three new stripped provenance attributes — the linked hazard's CONTENT never enters the hash (pinned), while weights/levels/grid/all three transforms move it (a weight edit re-rolls seeds even in joint mode, where weights are inert — mode is external state); registration (`ResponseFunctionType.Bivariate`, resolver-threading factory case, kitchen-sink entry) and the decision-10 guards — `CompositeResponse` DUAL (Validate + ThrowIfUnusable), `ProbabilitySource.Validate` for trees. 73 unit tests. V arrives with the S6 engine (`BivariateRiskVerification` incl. the collapse-vs-joint consistency cross-anchor, S6–S7) |
| Later | RFAHazard, `CompositeHazard` parameter-set import, BestFit imports, LifeSim | — | Phase 9 follow-on, Phase 11B |

## Critical Quality Standards

Zero tolerance: **no compiler errors, no compiler warnings, no empty catch blocks, proper exception propagation** (never silently swallow).

**XML documentation is MANDATORY on ALL types and methods — public AND private.** Required tags as applicable: `<summary>`, `<param>`, `<returns>`, `<exception>`, `<remarks>`. Do not suppress the CS1570–CS1591 doc warnings to pass a build; fix the docs. Every class in the **library and Verification projects** carries the Authors block in `<remarks>` (test classes are exempt by policy, ruled 2026-08-17; `validate-code-xml-docs.ps1` enforces the block and the phase/process-language ban in both source and the technical-reference/verification doc trees):

```csharp
/// <remarks>
/// <para>
///     <b>Authors:</b>
///     Haden Smith, USACE Risk Management Center, cole.h.smith@usace.army.mil
/// </para>
/// </remarks>
```

**No file-header banners.** Files start with `using` directives. The USACE notice lives in `LICENSE` only.

**Phase and process language is confined to the planning docs (ratified 2026-07-29).** Phase numbers, landing dates, ratification narration, and session language live only in `docs/ROADMAP.md`, `docs/PROGRESS.md`, this file, and the requirements docs' version-history blocks. Code comments (XML docs included), `docs/technical-reference/`, and `docs/verification/` describe the software as it is; verification pages keep dated runs-of-record (date, commit, counts), never phase tags.

**Never hand-roll numerics that Numerics provides.** Interpolation → `Numerics.Data.Interpolation.Linear`/`Bilinear`; distributions → `Numerics.Distributions`; sampling → `LatinHypercube`/`Stratify`/`BootstrapAnalysis`; root finding → `Brent`; **1D integration → `AdaptiveGaussKronrod`** (G10K21; replaces the v1.0 `AdaptiveSimpsonsRule` per [technical-reference/risk-integration.md](docs/technical-reference/risk-integration.md)), multi-D integration → `Vegas` (with its power-transform tail focus). **Single exception:** verification oracles in `RMC.TotalRisk.Verification` intentionally re-implement engine math from Numerics primitives — that independence is what makes them oracles.

**Validation contract:** every model type implements `public (bool IsValid, List<string> ValidationMessages) Validate()`. Messages start `"Error: ..."` (invalidating) or `"Warning: ..."` (advisory); `IsValid` is false only on errors.

**XML serialization contract:** element name `nameof(ClassName)`; doubles written `value.ToString("G17", CultureInfo.InvariantCulture)`; reads via `double.TryParse(s, NumberStyles.Any, CultureInfo.InvariantCulture, ...)` with null-safe attribute access. Every serializable class gets a round-trip unit test. **`ToXElement()` is also the canonical-hash identity surface** — serialized attribute names and owned-child order are append-only contract (see the landing checklist above).

**Property change notification:** model types implement `INotifyPropertyChanged` directly (`RaisePropertyChange(nameof(...))` after mutation). No WPF dependency.

**Numerical safety:** `double.NegativeInfinity` for impossible log-likelihoods (not `double.MinValue`); guard `Math.Log(x)` with `x > 0`.

**Namespaces:** library code uses block namespaces; test code uses file-scoped namespaces. No types in the bare `RMC.TotalRisk` root namespace. The legacy flat `TotalRisk` namespace must not survive a port.

## Verification Workflow

The legacy `Test_TotalRisk` suite is a set of **hand-rolled Monte Carlo oracles with fixed seeds and no asserts** (results were printed to the debugger). Conversion pattern, per scenario (full policy: `docs/verification.md`):

1. Port the oracle math to C# in `RMC.TotalRisk.Verification`, preserving the legacy fixed seeds (`MersenneTwister(12345)`/`(45678)`; MVN seeds 12345/67891/78910) at **1,000,000 realizations** (legacy used 10M; the tolerance scales).
2. Run the **new engine** on the same typed scenario.
3. `Assert` engine vs oracle within a statistical tolerance derived from the Monte Carlo standard error (SE ≈ σ/√N; at 1M ≈ 0.1% relative on means; document the k·SE factor per output). Engine seeds are content-based, so engine-vs-oracle is **statistical, never bit-exact**.
4. Pin engine reproducibility separately: same seed → bit-identical; shuffle/rename/reorder → bit-identical (the v1 seed-dependency bug regression).
5. Oracle values may additionally be pinned as constants once captured (exact at fixed seed + N) for regression.

Run verification **deliberately**: the family you touched after touching it; the full suite on demand. It is excluded from Release builds and from the fast PR gate by construction.

## Numerics API Gotchas

Verified traps to remember (extend as discovered):

- **`LogNormal` is base-10.** The natural-log lognormal is `LnNormal` — the legacy VB engine and oracles use `LnNormal`.
- `LatinHypercube.Random(n, d, seed)`: a seed ≤ 0 falls back to wall-clock — always pass an explicit positive seed.
- `UnivariateDistributionFactory.CreateDistribution` is a closed switch (`Mixture`/`CompetingRisks` reconstruct via their own `FromXElement`).
- `EmpiricalDistribution` historically did not round-trip its X/P tables through the base `ToXElement()` — **fixed upstream in the Phase 8 numerics work (N5, unreleased)**: table-bearing `ToXElement`/`FromXElement` overrides now exist (KernelDensity too) and are wired into `UnivariateDistributionFactory`; the model types' interim underlying-paired-data serialization remains valid and untouched.
- **`AdaptiveGaussKronrod`'s source file is misspelled `AdaptiveGuassKronrod.cs`; the *type* is spelled correctly.** It exposes the same surface the engine used on `AdaptiveSimpsonsRule` — `Integrate()`, `Integrate(List<StratificationBin>)`, `MaxDepth`, `MaxFunctionEvaluations`, `RelativeTolerance`, `ReportFailure`, `StandardError` — so the swap is drop-in. G10K21 nodes are strictly interior (max |x| ≈ 0.9957 < 1), so adjacent stratification bins never share an evaluation point from node placement — but that does **not** make the recorded set duplicate-free: a saturating hazard CDF collapses several bins onto one probability and `Integrate(List<StratificationBin>)` then evaluates a zero-width bin 21 times at one abscissa. `Function` is `Func<double,double>`; the acceptance-aware **`Recorder`** (N7) is an optional `(x, weight, f)` ledger flushed only for ACCEPTED intervals (Σweights = domain width; Σw·f ≡ Result; off by default with zero overhead), **adopted by the engine at Phase 8.5** via `Results/QuadratureMassLedger` — it keys on the exact abscissa and sums weights per key, so the degenerate bin is credited once at its true zero width. `MinDepth` defaults to 0; set ≥ 2. Two gates guard the adoption: `IntegrateComponent` checks the compensated weight total against Σ bin widths, and each curve checks that its matched abscissa count equals `DistinctAbscissaCount`. **`BuildHazardBins` retains the sampled hazard's natural probability support.** The 1D engine and `ProbeAnnualFailureProbability` add explicit lower and upper endpoint rectangles around the unchanged AGK interior weights, making the exhaustive mass exactly one without stretching an interior bin; the probe therefore matches production mass accounting before it drives the joint path's VEGAS tail-focus γ.
- **`Probability` lazy exclusive enumeration is the engine contract for wide failure-mode sets.** `IndependentExclusiveLazy`, `PositivelyDependentExclusiveLazy`, and `ExclusivePCMLazy` preserve subset-size/lexicographic order, the existing formulas and sign association, the closing half-gap row, the dual convergence predicate, and the `1E-4` defaults while reusing caller-owned rows. TotalRisk applies a sequential remaining-budget clip to emitted cells before consequences, profiles, and contributions; it never proportionally normalizes. The public dense `SystemComponent` inspection matrices are compatibility-only and must never return to an execution path.
- **`EmpiricalDistribution.Convolve(...)` (FFT) samples continuous *PDFs* on a *linear* uniform grid** over `[Σmin, Σmax]` with `fftPoints = NextPowerOfTwo(max(8·numberOfPoints, 2048))` — so a point mass (a CDF jump, e.g. the zero-inflation atom of a defective risk curve) **has no representation**: any ramp approximation loses the atom or corrupts the normalization. The additive system engine convolves exactly on a shared lattice via `Fourier.FFT` instead (`SystemConvolution`); Numerics item N8 covers both the log-spaced grid and the atom-aware overload. The linear grid also starves order-of-magnitude tails — pass `numberOfPoints ≥ 4096` and cross-check the convolved mean against `Σ` component means.
- **`Vegas.TailFocusParameter` (γ) concentrates samples in the upper tail** via `p' = 1 − (1−p)^γ`; `ConfigureForRareEvents(pTarget)` sets `γ = ln(pTarget)/ln(0.05)` clamped [1, 20]. In TotalRisk the Vegas `wgt` *is* the LEC probability mass, so the power-transform Jacobian must be folded into `wgt` or every LEC ordinate is biased even when the returned integral is correct — verify before enabling γ > 1 (Numerics item N9).
- Verification data files (when they exist) are read relative to `AppContext.BaseDirectory`, never the repo path, and parsed with `CultureInfo.InvariantCulture`.
- **`Statistics.ParallelMean` is order-nondeterministic** (PLINQ `AsParallel().Sum()`, partitioned by core count) — the engine's ensemble reductions and percentile means sum sequentially instead; any in-loop shared accumulation breaks the bit-identical-at-any-thread-count contract (the `EngineReproducibilityVerification` repeated-run pin is the tripwire). **The library has no remaining call sites** — the last one (`TabularHazard`'s PERT-percentile-Z mean curve, which feeds every realization) was replaced by a sequential reduction at Phase 8.5.
- **`MultivariateNormal`'s CDF above two dimensions is a RANDOMIZED lattice rule** (Genz's MVNDST) drawing from its own `MVNUNI` generator, so its result carries a stochastic error near the requested tolerance and is not reproducible unless the generator is seeded. Numerics defaulted it to a **clock-seeded** `MersenneTwister` until Phase 8.5 (N17); it now defaults to `MultivariateNormal.DefaultMVNUNISeed` and `CompetingRisks` exposes a `PRNGSeed` that TotalRisk drives from the component's content seed. Two consequences that still hold: **at two dimensions a closed bivariate form is used**, so a D = 2 fixture cannot detect a seeding regression (use ≥ 3); and `MVNUNI` is advanced by evaluation, so an instance shared across threads must be cloned per thread.
- **A Numerics interpolator is not safe for concurrent reads.** `Interpolater.SearchStart` is mutable state written on every lookup (`UseSmartSearch` defaults true, the correlated-search accelerator), so sharing one `EmpiricalDistribution`/`Linear`/`OrderedPairedData`-backed instance across the parallel realization loop is a data race — and on a non-strict curve, where ties admit more than one valid bracket, a corrupted search start can return a *different ordinate*, not merely a slower lookup. Share the underlying `double[]` and rebuild the wrapper per realization; `OrderedPairedData`'s two-list constructor copies rather than retaining or sorting its inputs, which is what makes that safe.
- **`EmpiricalDistribution(IList, IList)` requires strictly increasing probabilities** — a fragility that plateaus at 0/1 (every saturating fragility) must construct through a non-strict `OrderedPairedData(..., yStrict: false, Ascending)`. `CompetingRisks.CumulativeIncidenceFunctions` builds its *outputs* with the strict two-list ctor, so the engine rebuilds each CIF non-strict before querying (Numerics follow-up item alongside N7–N9).
- **The Vegas power-transform Jacobian IS folded into the weight handed to the integrand** (`Vegas.cs:466-472`) — the recorded `wgt` mass is Jacobian-corrected; the 4b γ-audit (`SystemRiskVerification.Test_JointSystem_TailFocusAudit`) is green, so γ > 1 is safe to enable (the N9 upstream unit tests shipped in the Phase 8 numerics work: γ ∈ {1,4,10} unbiased, Σwgt = volume per batch).
- **`Vegas.UseSobolSequence` defaults `true`** — the UNRANDOMIZED driving points would then be seed-independent (the `Random` property is ignored), voiding the content-seed contract; the engine sets it `false` with a seeded `MersenneTwister` by default. Since 2026-08-28, `Vegas.SobolSeed` (nullable) selects the Matousek-scrambled sequence instead — seeded, reproducible — and the opt-in hashed `RiskAnalysisOptions.UseSobolJointSampling` drives the joint path with it at the same content-derived `vegasSeed` (conditional attribute; default false = the MT driver verbatim). **And `Vegas.NumberOfBins`'s setter reallocates every internal array including the importance grid** — `ConfigureForRareEvents` raises the bin count, so tail-focus configuration must happen **before** the warm-up or the warmed grid is wiped (the reason the automatic γ target comes from the deterministic AGK failure-probability probe, not a warm-up harvest).
- **`Probability.IndependentExclusive`'s convergence shortcut can truncate the deepest combinations** (it appends one closing pseudo-row of half the inclusion–exclusion gap and returns early when the union converges within 1e-4) — so `Σ` exclusive probabilities can fall short of 1 by up to the tolerance on high-dimensional systems; the engine's mass-balance witness surfaces the dropped mass honestly.
- **Results JSON**: System.Text.Json shortest-round-trip doubles are bit-exact; NaN/±Infinity need `JsonNumberHandling.AllowNamedFloatingPointLiterals` (the shared `ResultsJson` options). Compare compressed payloads post-decompression — GZip headers embed non-content fields.
- **`ParametricUnivariateHazard`/`ParametricResponse` require `Estimate()` before sampling** (they carry a BestFit-style estimation lifecycle); a bare `ParentDistribution` assignment validates as unestimated, and `Estimate()` makes them *uncertain* (bootstrap posterior) — deterministic fixtures use tabular functions.
- **The §5.5.8 seed pin removes seed noise only, never quadrature adaptivity** (Phase 6.6): the 1D AGK mesh follows the configured `RiskIntegrand` and the joint VEGAS integrand is inherently consequence-bearing, so a perturbation that moves integrand values shifts results within integration tolerance even with `PinnedSamplerSeeds` set — a deterministic parameter effect. Bit-identity fixtures pair the pin with `RiskIntegrand.TotalProbabilityOfFailure` (1D, consequence perturbations) or a hashed-but-integrand-inert edit like `HazardThreshold` (joint) — see `SamplerSeedMapTests`.
- **`HazardLevelSensitivity` re-runs `SetupSamplers` at its own realization count** (default 100) — sampler state is mutated as a documented side effect; `RunAsync` and `MeasureSensitivity` re-seed defensively, but a test reading sampled functions directly after a sensitivity call sees the sensitivity design, not the run's.
- **`NonparametricHazard` gates sampling on INPUT validity, never on the derived table's strict-dominance flag** — the σ repair enforces the 1% confidence bound only, so a legitimately derived LnNormal ladder can cross deep in the tails (`UncertainOrderedPairedData.IsValid` false is normal there; v1.0 never consulted it either, and sampled curves repair via `ForceMonotonic`). And `Tools.Log` floors only small POSITIVES (negatives pass through to NaN), so the log-axis derivation gate must reject negative hazard ranges before logging.
- **`BootstrapAnalysis.Estimate()` is bit-reproducible as of Phase 8.5 (N12)** — it was not before: the summary reductions merged thread-local partials through `Tools.ParallelAdd`, so two calls with identical inputs and an identical `PRNGSeed` agreed numerically but not bit-for-bit, leaving an unstable canonical hash on any container that folds a child hash. Every reduction over replications now sums over a fixed chunk count, independent of the thread count. `CompositeHazardVerification.Test_UpstreamEstimation_IsBitReproducible` pins it. N12 also fixed a bias in `ExpectedProbabilities` (it divided by the full replication count while skipping failed fits) and surfaced failures through `BootstrapAnalysis.FailedReplications` — a non-zero count means the results rest on fewer samples than requested. The `NonparametricHazard`/`TabularHazard` mean-curve note above is likewise resolved.
- **`Mixture.CreateEmpiricalCDF()` changes `InverseCDF` results** by roughly 3 × 10⁻³ of the exceedance probability — it is a ~200-bin interpolation, not a refinement of the Brent solve. The composite hazard/response types build it for v1.0 parity (the risk integrand inverts at every quadrature node, so interpolating is the point), but an oracle comparing against a plain `Mixture` must build it too or it is comparing two different inversion paths.
- **`Bilinear` lives in `Numerics.Data` (not `.Interpolation`) and is NOT safe for concurrent reads** — like every Numerics interpolator it carries mutable correlated-search state, so the bivariate types build one fresh configured instance per sampled realization sharing the function's arrays (never per call, never shared, nothing cached on the function).
- **A copula's `InverseCDF(u, v)` second argument is a CONDITIONAL probability**, not a joint coordinate: the array form returns `[u, InverseConditionalCDF(u, v)]` — v is the conditional level t of the h-function inverse. Feeding it a marginal non-exceedance produces distributionally plausible garbage.
- **`CopulaType` is append-only serialized contract** — copulas serialize by enum NAME through `<Copula Type Parameters/>`; renaming or reordering members breaks stored forms (Independence was appended last, value 7).
- **Zero-parameter copulas break the copula estimation path** — `BivariateCopulaEstimation.Estimate` returns immediately for them (the guard landed with `IndependenceCopula`); anything walking `ParameterConstraints`/θ machinery must special-case a parameterless copula.
- **The Ali-Mikhail-Haq copula's doc comments misspell it "AHM"** — the class is `AMHCopula` and the enum member `AliMikhailHaq`, but the XML summaries say "Ali-Mikhail-Haq (AHM)", so a text search for "AMH" misses the doc surface and "AHM" misses the identifiers.
- **Only `GumbelCopula` and `JoeCopula` solve their conditional inverse numerically** (Brent over [0, 1]); every other family has an analytic inverse. Both used to throw "root is not bracketed" at conditional levels within a ulp of 1 — algebraically h(1|u) ≡ 1, but the floating-point objective rounds below it — which crashed any engine run through them, since the conditional bins always include that clamp node. **Fixed upstream (2026-08-07)** by saturating at the boundary when the level is unbracketed within rounding. Related, deliberately unchanged: the Archimedean families inherit `ConditionalCDF` as the generator ratio φ′(u)/φ′(C(u,v)) rather than the closed form their inverse solves, and it degrades at v = 1 for large θ (Joe returns 0.5245 at θ = 20, u = 0.846 where the exact value is 1) — never round-trip the inverse against `ConditionalCDF` at the exact boundary.
- **Cascades (Phase 6.7, arch doc §7.9):** end states classify by FINAL polarity — a Non-Fail-final terminal is a claimed non-failure state whose mode-scope curves live in its **NonFail** stream (Fail/Excess empty) and whose mass rides the complement conditionally; the component **Background stream becomes the claimed + remainder mixture** whenever claimed states exist (bit-identical to the raw background otherwise). The `ResponseNodes` attribute exists in the **identity form only** (never persisted) — it distinguishes one shared response element from equal-content duplicates, which compute differently. `SampledFailureMode.InverseSRP` throws for multi-stage/Non-Fail modes (polarity products are non-monotone); Competing errors only on **else-chain** failure states (a Fail-final state riding a Non-Fail branch — progression cascades compete fine); claimed states are limited to one state group per component. Combination caches/MVN/correlation dimension = the `EndStateGroupLayout` **combination-unit count** (the failure-path count for every pre-6.7 layout).

## Code Conventions

- Each public class in its own `.cs` file; file name matches class name; folders mirror namespaces; enums one per file.
- Comments explain *why*, not *what*. No commented-out code.
- No LINQ or allocations inside per-realization compute paths; pre-allocate in setup, index in the loop.
- PascalCase types/properties, `_camelCase` private fields, camelCase locals/parameters.
- `#region` grouping when a class is large: `Construction`, `Members`, `IModel Methods`, `Private Helpers`, `Serialization`.
- Tests: files `<ClassName>Tests.cs` mirroring `src/`; methods `Test_<Scenario>_<ExpectedResult>`; `// Arrange / // Act / // Assert` comments; explicit deltas on floating-point asserts.

## Build Commands

```bash
dotnet build                                        # full solution, Debug — zero warnings required
dotnet test -c Release                              # fast PR gate (Verification skipped)
dotnet test src/RMC.TotalRisk.Tests                 # fast loop, Debug
dotnet test src/RMC.TotalRisk.Verification          # verification suite (deliberate)
powershell -File scripts/validate-code-xml-docs.ps1 # doc coverage + namespace/dependency guards
py scripts/sync-agents-md.py                        # regenerate AGENTS.md after CLAUDE.md edits
```

## Permissions and Approval (MANDATORY)

**Codex MUST NOT perform code changes without explicit user approval.** Present a plan first and wait for approval. Never start a major refactor autonomously. Small, clearly-scoped tasks may proceed after confirming scope. If in doubt, ask.

## Session Workflow

1. User assigns a phase or task (one roadmap phase per session).
2. Codex reads `docs/PROGRESS.md` + `docs/ROADMAP.md`, presents a plan, and waits for approval.
3. Work follows the porting loop: port → unit tests → verification (when the phase includes it) → `dotnet build` (0 warnings) → `dotnet test -c Release` → `validate-code-xml-docs.ps1` → update the Ported Types Matrix + `docs/PROGRESS.md` → regenerate AGENTS.md if CLAUDE.md changed → commit.
4. User compiles/reviews; fix; repeat.

## Git Workflow

- All v1.1 work lands on **`v1.1-development`**. `main` remains the v1.0-era public face until v1.1 ships; the user coordinates merges and pushes.
- **Committing is mandatory** after a successful validation run — commit before reporting the work complete. Stage only files belonging to the change. If validation fails, do not commit.
- **No AI breadcrumbs in commits**: no generated-by text, no co-authored-by AI trailers, no references to AI assistants or agents by name.
- **Do not push unless explicitly asked.** Never push to `main`.

## Progress Tracking

Track completed work in `docs/PROGRESS.md` (newest first: **Goal / Landed / Verified / Next**, with exact gate counts and user decisions recorded). Update the Ported Types Matrix in this file as types reach P/T/V status.

## Files to Ignore

Never commit: `**/bin/`, `**/obj/`, `**/.vs/`, `**/TestResults/`, `*.user`, `*.suo`, local NuGet caches.

## Future Feature Enhancements

Planned follow-on projects (RMC.TotalRisk.UI WPF layer, the RMC-TotalRisk desktop app shell, RMC.TotalRisk.Api REST API, FDA importer) are documented in CLAUDE.md only — see that file for scope and design criteria.
## Unit Test API Gotchas (CRITICAL)

- **`dotnet test` on .NET 10 (Microsoft.Testing.Platform):** `--filter` before `--` is silently ignored for MSTest.Sdk projects; scope by project path or configuration. A filter that matches zero tests in an assembly FAILS that assembly.
- **`RiskAnalysisOptions.UseDefaults` defaults `true` and `RunAsync` re-applies `SetIntegrationDefaults` at run start** — in-test integration knobs (`Tolerance`, `EnsembleTolerance`, `EnsembleMinDepth`, VEGAS budgets) are silently overwritten unless the fixture sets `UseDefaults = false` first. Two legacy verification fixtures carried ineffective 1e-6 relaxations for this reason (found and pinned at Phase 6.5).
- **Ensemble discipline split (Phase 6.5):** ensemble realizations integrate at `EnsembleTolerance`/`EnsembleMinDepth` (defaults 1e-4/0); the mean pass, probes, and mean-only runs use `Tolerance`/MinDepth-2 (1e-8). A test comparing per-realization values to exact quadrature (or across the two paths) must pin the ensemble discipline in-test with a documented rationale — never re-pin captured literals to relaxed-discipline values silently.
- Verification tolerances are documented per test with their derivation (k·SE); never widen a tolerance to make a test pass without recording why in the test's XML docs.
- MSTest method-level parallelization is on: no shared mutable statics, no order dependence, no `Thread.Sleep` timing assumptions.
